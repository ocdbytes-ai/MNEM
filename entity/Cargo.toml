[package]
name = "entity"
version = "0.1.0"
edition = "2024"

[dependencies]
types = { path = "../types" }
resvg = "0.44"
tiny-skia = "0.11"
noise = "0.9"

[target.'cfg(target_os = "linux")'.dependencies]
interface = { path = "../interface" }
rppal = "0.22.1"
